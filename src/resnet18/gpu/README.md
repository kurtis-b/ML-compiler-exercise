Use compile.sh to get the binary a.out --> This prints the output
Use benchmark.sh to get the binary bench.out --> Benchmark the function call (Currently I can only call the function once)

Use `watch -n 0.5 nvidia-smi` to monitor the GPU usage.